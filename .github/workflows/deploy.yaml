name: Deploy Hugo site

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  call:
    name: Call
    permissions:
      id-token: write
      pages: write
    uses: Anthrocon/action-deploy/.github/workflows/deploy.yaml@main
    with:
      hugo-version: "0.106.0"
    secrets: inherit
